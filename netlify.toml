# Netlify configuration for Assembly Game
# Run the build inside the `assemly-game` subfolder and publish its `dist` folder.
[build]
  base = "assemly-game"
  command = "npm run build"
  publish = "dist"
